追加で1点だけ確認してください。

app/stage2/page.tsx で取得した strategyId が、現在の companyId と一致する strategy_data.id であることを確認してください。

確認項目：
- useStrategyStore の strategyId は loadAndHydrate 後に現在の companyId の strategy_data.id になっているか
- 会社切替時に古い strategyId が残らないか
- handleGenerateFinal 実行直前に companyId と strategyId が両方存在するか
- 存在しない場合はAPIを呼ばず、画面に「戦略データIDを取得できません。再読み込みしてください」と表示するか

可能なら handleGenerateFinal の直前に以下のガードを入れてください。

const current = useStrategyStore.getState();
if (!current.companyId || !current.strategyId) {
  setError('戦略データIDを取得できません。ページを再読み込みしてください。');
  return;
}

この確認後、STAGE2で実際に最終ストーリー生成を再実行し、
エラーが出ないことと、意図しない過去データが出ないことを確認してください。