確認を進めてください。

特に ask-ceo-agent は利用範囲が広いため、いきなり修正せず、以下を先に確認してください。

1. 呼び出し元の特定
- ask-ceo-agent を呼んでいるフロント/UI/APIをすべて検索
- fetch('/api/ask-ceo-agent')
- /api/ask-ceo-agent
- ask-ceo-agent
で検索してください。

2. payload確認
各呼び出し元で以下が送られているか確認してください。
- userId
- strategyId
- companyId
- messages
- meta

特に strategyId が常に送られているか確認してください。

3. strategyId が送られていない呼び出し元がある場合
以下を報告してください。
- ファイル名
- 画面/機能名
- なぜ strategyId がないのか
- useStrategyStore から strategyId を取得できるか
- access / membership / loader から補完しているのか

4. ask-ceo-agent 側の現在処理確認
- request body で strategyId を必須にしているか
- strategyId がない場合でも companyId で取得するfallbackがあるか
- assertCompanyScopeByStrategyId が必ず呼ばれているか
- その後に getFullStrategyDataByCompany(companyId) で取り直していないか

5. 修正方針案を出してください
原則として ask-ceo-agent は以下に変更したいです。

- strategyId 必須
- membership.companyId を会社スコープにする
- strategy_data は strategyId + membership.companyId で取得
- getFullStrategyDataByCompany(companyId) は使わない
- getFullStrategyDataByStrategyId(strategyId, companyId) 新関数を使う
- companyId だけのfallbackは禁止

6. 互換性確認
strategyId 必須化で壊れる画面がある場合は、先にフロント側で strategyId を送る修正案を出してください。
API側で companyId fallback を残す対応は避けてください。

確認結果を一覧で報告してください。
その後、ask-ceo-agent を修正してください。