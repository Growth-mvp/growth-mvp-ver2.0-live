まだ駄目です。原因は normalize ではなく、リロード時の fetch/select で `stage3_strategy_bridge` を取得できていない可能性が高いです。

## 根拠

以前のログでは：

```ts
[DB verify saved bridge expanded]
hasStrategicCore: true
keys: Array(7)

つまりDB保存は成功しています。

しかしリロード後：

[STAGE3 bridge render]
hasStrategicCore: false
concreteDomains: undefined
nonNegotiableThemes: undefined

さらに現在のログでは buildStateFromDbRow が大量に出ていますが、stage3_strategy_bridge をDB rowから読んだログが見えません。

修正対象

fetchStrategy / Supabase select 文を確認してください。

おそらく strategy_data 取得時の .select(...) に stage3_strategy_bridge が含まれていません。

必須修正

utils/supabase/strategy.ts の strategy_data fetch/select に、必ず以下を含めてください。

stage3_strategy_bridge

例：

.select(`
  id,
  company_id,
  ...
  stage3_strategy_bridge,
  ...
`)

もし select('*') ではなく明示列挙なら、そこに追加してください。

追加ログ

buildStateFromDbRow の最初にこれを入れてください。

console.log('[buildStateFromDbRow] stage3 bridge raw from DB', {
  hasColumn: Object.prototype.hasOwnProperty.call(row, 'stage3_strategy_bridge'),
  hasBridge: !!row.stage3_strategy_bridge,
  keys: row.stage3_strategy_bridge ? Object.keys(row.stage3_strategy_bridge) : [],
  hasStrategicCore: !!row.stage3_strategy_bridge?.strategicCore,
});
判定

リロード直後に以下になればOKです。

[buildStateFromDbRow] stage3 bridge raw from DB
hasColumn: true
hasBridge: true
hasStrategicCore: true

その後：

[STAGE3 bridge render]
hasStrategicCore: true
重要

DB保存はもう疑わないでください。
[DB verify saved bridge expanded] hasStrategicCore: true なので保存はできています。

今の本丸は、
リロード時の fetch/select が stage3_strategy_bridge カラムを取得しているか
です。